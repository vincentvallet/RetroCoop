import argon2 from 'argon2'; const email=process.env.ADMIN_EMAIL, password=process.env.ADMIN_INITIAL_PASSWORD; if(!email||!password||password.length<10)throw new Error('ADMIN_EMAIL et un mot de passe de 10 caractères minimum sont requis.'); console.log(`Hash Argon2id généré pour ${email} (${(await argon2.hash(password,{type:argon2.argon2id})).length} caractères). Utilisez la commande avec DATABASE_URL pour créer le compte.`);
