'use client';
import {FormEvent,useState} from 'react';

export default function EmailPreference({initialJoin,initialLeave}:{initialJoin:boolean;initialLeave:boolean}){const [status,setStatus]=useState('');async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setStatus('Enregistrement…');const form=new FormData(event.currentTarget),response=await fetch('/api/account/preferences',{method:'PATCH',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({joinEmailEnabled:form.get('joinEmailEnabled')==='on',leaveEmailEnabled:form.get('leaveEmailEnabled')==='on'})}),result=await response.json();setStatus(response.ok?result.message:result.error)}return <form className="account-form" onSubmit={submit}><label className="preference-check"><input name="joinEmailEnabled" type="checkbox" defaultChecked={initialJoin}/>Recevoir un email lorsqu’un joueur rejoint l’une de mes sessions</label><label className="preference-check"><input name="leaveEmailEnabled" type="checkbox" defaultChecked={initialLeave}/>Recevoir un email lorsqu’un joueur quitte l’une de mes sessions</label><button className="btn secondary">Enregistrer les préférences</button>{status&&<p role="status">{status}</p>}</form>}
