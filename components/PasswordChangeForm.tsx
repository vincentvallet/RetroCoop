'use client';
import {FormEvent,useState} from 'react';

export default function PasswordChangeForm(){const [status,setStatus]=useState('');async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget,data=new FormData(form);setStatus('Mise à jour…');const response=await fetch('/api/account/password',{method:'PATCH',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({currentPassword:data.get('currentPassword'),newPassword:data.get('newPassword'),confirmation:data.get('confirmation')})}),result=await response.json();setStatus(response.ok?result.message:result.error);if(response.ok)form.reset()}return <form className="account-form" onSubmit={submit}><label>Mot de passe actuel<input name="currentPassword" type="password" autoComplete="current-password" maxLength={128} required/></label><label>Nouveau mot de passe<input name="newPassword" type="password" autoComplete="new-password" minLength={12} maxLength={128} required/></label><label>Confirmer le nouveau mot de passe<input name="confirmation" type="password" autoComplete="new-password" minLength={12} maxLength={128} required/></label><p className="form-help">Au moins 12 caractères, une lettre et un chiffre. Le collage et les gestionnaires de mots de passe sont autorisés.</p><button className="btn">Changer mon mot de passe</button>{status&&<p role="status">{status}</p>}</form>}
